let myHeading = document.querySelector("h1");
function toggle_info1() {
  const foo = document.getElementById("cat1");
  // 取得 `foo` 的 class 列表。
  // `toggle` 會賦予元素給定的 class，或是移除給定的 class，
  // 取決於 class 是否已經存在
  foo.classList.toggle("invisible");
}
function toggle_info2() {
  const foo = document.getElementById("cat2");
  // 取得 `foo` 的 class 列表。
  // `toggle` 會賦予元素給定的 class，或是移除給定的 class，
  // 取決於 class 是否已經存在
  foo.classList.toggle("invisible");
}
function toggle_info3() {
  const foo = document.getElementById("cat3");
  // 取得 `foo` 的 class 列表。
  // `toggle` 會賦予元素給定的 class，或是移除給定的 class，
  // 取決於 class 是否已經存在
  foo.classList.toggle("invisible");
}
